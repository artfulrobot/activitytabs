<h3>This new page is generated by CRM/Activitytabs/Page/Activitytabs_Tab.php</h3>

<table>
  <thead>
    <tr>
      {foreach from=$atabConfig->columns item=col key=i}
        <th>{$col}</th>
      {/foreach}
    </tr>
  </thead>
</table>
{foreach from=$activities name=activityloop key=i item=activity}
  <p>hello "{$activity.subject}" </p>
{/foreach}
<p>CONF: {$atabConfig->columns[0]}</p>
{debug}
{* Example: Display a variable directly *}

<p>{ts 1=$currentTime}(In your native language) The current time is %1.{/ts}</p>
